@using AFRICOMDMSWEB.Data
@using AFRICOMDMSWEB.Models.ViewModel
@model ReqPRocessViewModel
<partial name="sidebbar" />
<div class="home-section">
    <div class="contain">
        <div class="cont">
            <form>
                <div class="form-group p-2 col-6">
                    @*<label asp-for="requestAction.ActionId"></label>*@
                   <select asp-for="reqProcess.ReqtypeId" asp-items="@Model.selectRequestType" class="form-control">
                        <option disabled selected>--Select Action--</option>
                    </select>
                </div>

                <div class="form-group p-2 col-6">
                   @* <label asp-for="requestAction.ActionId"></label>*@
                 <select asp-for="reqProcess.RoleId" asp-items="@Model.selectPearentRole" class="form-control">
                        <option disabled selected>--Select Role--</option>
                    </select>
                </div>

                <div class="form-group p-2 col-6">
                    <select asp-for="reqProcess.ParentRoleId" asp-items="@Model.selectPearentRole" class="form-control">
                        <option disabled selected>--Select Higher Role --</option>
                    </select>
                </div>

              
                    <div class="form-group col-3 p-2">
                        <button type="submit" class="btn btn-primary">Create</button>
                    </div>
            
            </form>
        </div>
    </div>
</div>